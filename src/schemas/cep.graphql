type Cep {
  cep: String
  logradouro: String
  complemento: String
  bairro: String
  localidade: String
  uf: String
  ibge: String
  gia: String
  ddd: String
  siafi: String
}

type Query {
  cep(cep: String!): Cep!
}
